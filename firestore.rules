rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SADECE GELİŞTİRME İÇİN. Canlıda bu kurallar açık bırakılmamalıdır.
    match /teachers/{id} { allow read, write: if true; }
    match /attendance/{id} { allow read, write: if true; }
  }
}
